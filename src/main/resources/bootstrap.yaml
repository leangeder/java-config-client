spring:
  cloud:
    config:
      uri: http://config-server:8080
      fail-fast: true
      retry:
        initial-interval: 1000
        maxInterval: 3000
        maxAttempts: 30
        multiplier: 1.1